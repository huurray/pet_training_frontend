import React, {FunctionComponent} from 'react';
import {Text, View} from 'react-native';

const MyPage: FunctionComponent<void> = ({}) => {
  return (
    <View>
      <Text>마이페이지</Text>
    </View>
  );
};

export default MyPage;
