import React, {FunctionComponent} from 'react';
import {Text, View} from 'react-native';

const Training: FunctionComponent<void> = ({}) => {
  return (
    <View>
      <Text>트레이닝</Text>
    </View>
  );
};

export default Training;
